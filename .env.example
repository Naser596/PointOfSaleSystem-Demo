APP_PORT=5088
ASPNETCORE_ENVIRONMENT=Development

# PostgreSQL is the Docker/default database provider.
POSTGRES_PORT=5432
POSTGRES_DB=pos_platform
POSTGRES_USER=pos_user
POSTGRES_PASSWORD=change_me

# Optional local fallback when running without PostgreSQL.
POS_DB_PATH=pos.db
